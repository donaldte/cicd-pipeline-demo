<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>tech.squadplus</groupId>
  <artifactId>cicd-pipeline-demo</artifactId>
  <!--
    This number is intentionally static. The pipeline does not rewrite it:
    the real, meaningful version lives in git tags / GitHub Releases / the
    Docker image tag, computed by semantic-release from Conventional Commits
    (see .github/workflows/release-and-deploy-dev.yml and README.md).
  -->
  <version>0.0.0</version>
  <packaging>jar</packaging>

  <name>cicd-pipeline-demo</name>
  <description>
    Minimal Java service used to demonstrate a complete, professional
    CI/CD pipeline on GitHub Actions (CI gates, semantic release,
    automatic CD to dev, manual CD with approvals).
  </description>

  <properties>
    <maven.compiler.release>21</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <junit.version>5.10.2</junit.version>

    <!-- Plugin versions, pinned for reproducible builds. -->
    <compiler.plugin.version>3.13.0</compiler.plugin.version>
    <surefire.plugin.version>3.2.5</surefire.plugin.version>
    <jar.plugin.version>3.4.1</jar.plugin.version>
    <spotless.plugin.version>2.43.0</spotless.plugin.version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>${junit.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <finalName>${project.artifactId}</finalName>
    <plugins>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>${compiler.plugin.version}</version>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>${surefire.plugin.version}</version>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
        <version>${jar.plugin.version}</version>
        <configuration>
          <archive>
            <manifest>
              <mainClass>tech.squadplus.demo.Application</mainClass>
            </manifest>
          </archive>
        </configuration>
      </plugin>

      <!--
        "format" CI gate: `mvn spotless:check` fails the build if the code
        is not formatted; `mvn spotless:apply` fixes it in place. Kept as
        its own Maven goal (not bound to a lifecycle phase) so the CI job
        can run/report it as a clearly separate step.
      -->
      <plugin>
        <groupId>com.diffplug.spotless</groupId>
        <artifactId>spotless-maven-plugin</artifactId>
        <version>${spotless.plugin.version}</version>
        <configuration>
          <java>
            <googleJavaFormat/>
          </java>
        </configuration>
      </plugin>

    </plugins>
  </build>
</project>
